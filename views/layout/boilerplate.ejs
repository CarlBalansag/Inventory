<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> — Inventory</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/navbar', { currentUser: locals.currentUser || null }) %>
  <div class="wrap">
    <%- include('../partials/flash', { success: locals.success || [], error: locals.error || [] }) %>
    <%- body %>
  </div>
</body>
</html>
